# ========================================
# LOCAL DEVELOPMENT
# ========================================
PORT=5001
DB_NAME=binisha_db
DB_USER=bishalranjitkar
DB_PASS=
DB_HOST=localhost
DB_DIALECT=postgres

# ========================================
# RENDER DEPLOYMENT
# ========================================
# On Render, set these environment variables in your service settings:
#
# NODE_ENV=production
# PORT=10000
# DATABASE_URL=<paste Internal Database URL from Render PostgreSQL service>
#
# Example DATABASE_URL format:
# postgresql://user:password@host.render.com/database_name
#
# To seed the database from your local machine:
# 1. Temporarily add the EXTERNAL Database URL here
# 2. Run: node seed.js
# 3. Remove the DATABASE_URL from this file (don't commit it!)
#
# DATABASE_URL=